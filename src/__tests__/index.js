// @flow

import Layer, { Consumer, Manager, Provider } from "..";

test("Layer", () => {
  expect(typeof Layer).toBe("function");
});

test("Consumer", () => {});

test("Manager", () => {});

test("Provider", () => {});
